# ============================================
# ENVIRONMENT VARIABLES
# ============================================

# Node Environment
NODE_ENV=production

# CORS Settings (Optional)
# Comma-separated list of allowed origins
ALLOWED_ORIGINS=https://your-app.vercel.app,https://www.your-app.vercel.app

# ============================================
# INSTRUCTIONS
# ============================================
#
# This app uses in-memory storage - no database required!
# Data resets on each deployment/cold start.
#
# For Vercel deployment:
#    1. Install Vercel CLI: npm i -g vercel
#    2. Run: vercel --prod
#    3. Follow the prompts
#
# Optional environment variables can be set in Vercel Dashboard:
#    - Go to Vercel Dashboard > Your Project > Settings > Environment Variables
#    - Add NODE_ENV=production
#    - Add ALLOWED_ORIGINS with your domain
#
# For persistent storage in the future, consider:
#    - Vercel KV (Redis)
#    - Vercel Postgres
#    - Neon Database
#    - Supabase
#
# 4. NEVER commit .env to git!
#    (It's already in .gitignore)
#
